name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    env:
      USE_C_ALLOC: '0'
      WITH_PYTHON: '0'
    strategy:
      matrix:
        target: [ aarch64-linux, x86_64-linux ]
    steps:
      - uses: actions/checkout@v4
      - name: Install Zig
        uses: mlugg/setup-zig@v2
        with:
          version: 0.16.0

      - name: Build (matrix target)
        run: make build TARGET=${{ matrix.target }} OPT=ReleaseFast
      - name: Unit tests (Debug)
        run: make test

